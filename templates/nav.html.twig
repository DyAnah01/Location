<nav class="navbar navbar-expand-lg bg-body-secondary bg-secondary" data-bs-theme="dark">
  <div class="container-fluid">
      <a class="navbar-brand" href="#"></a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
          
        <li class="nav-item">
          <a class="nav-link active text-white mx-3" aria-current="page" href="{{ path('app_accueil')}}">Accueil</a>
        </li>
        {% if app.user %}

        {% if app.user.roles[0] == "ROLE_ADMIN" %}

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white mx-3" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Gestion de Locations
          </a>          
          <ul class="dropdown-menu bg-secondary mx-3">
            <li><a class="dropdown-item text-white" href="{{ path('app_agences') }}">Agences</a></li>
            <li><a class="dropdown-item text-white" href="{{ path('app_vehicule') }}">Vehicules</a></li>
            <li><a class="dropdown-item text-white" href="{{ path('app_membre') }}">Membre</a></li>
            <li><a class="dropdown-item text-white" href="{{ path('app_commande') }}">Commandes</a></li>
          </ul>
        </li>
        {% endif %}

        {% else %}
        <li class="nav-item">
          <a class="nav-link text-white mx-3" href="{{ path('app_contact') }}">Contact</a>
        </li>         
        {% endif %}

        <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            Mon Compte
          </button>
          <ul class="dropdown-menu dropdown-menu-dark">
            {% if app.user %}
              {% if app.user.roles[0] == "ROLE_ADMIN" %}
              <li><a class="dropdown-item text-white" href="{{ path('app_logout') }}">Se déconnecter</a></li>

              {% else %}
                <li><a class="dropdown-item text-white active" href="{{ path('historique', {'id': app.user.id}) }}">Historique de commande</a></li>
              <li><a class="dropdown-item text-white" href="{{ path('app_logout') }}">Se déconnecter</a></li>

               {% endif %}        
              {% else %}

              
            {% endif %}

            {% if app.user %}
            
            {% else %}
            <li><a class="dropdown-item active" href="{{ path('registration') }}">S'inscrire</a></li>
            <li><a class="dropdown-item" href=" {{ path('app_login') }} ">Se connecter</a></li>             
            {% endif %}
   
          </ul>
        </div>

        <li class="nav-item">
          <a class="nav-link text-white mx-3" href="{{ path('app_contact') }}">Contact</a>
        </li> 

      </ul>
      {% if app.user %}
        <div class="mb-3 text-white">
           <p>You are logged in as <span class="text-danger">{{ app.user.userIdentifier }}</span> </p> 
             {# <a href="{{ path('app_logout') }}" class="ms-5">Logout</a> #}
            {# {{ dump(app.user.userIdentifier) }} #}
        </div>
      {% endif %}
    </div>
  </div>
</nav>