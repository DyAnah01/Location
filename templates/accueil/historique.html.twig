{% extends "base.html.twig" %}

{% block body %}

{% if OneCommande %}
{# {{ dump(OneCommande) }} #}
<h2 class="text-center text-danger"> Liste de commande</h2>
{% for value in OneCommande %}
    <div class="card mb-3" style="max-width: 540px;">
        <div class="row g-0">
          <div class="col-md-4">
            <img src="{{value.photoVehicule}}" class="img-fluid rounded-start" alt="...">
          </div>
          <div class="col-md-8">
            <div class="card-body">
              <h5 class="card-title">{{value.titreVehicule}} - {{value.marque}} - {{ value.modele }}</h5>
              <p class="card-text">{{value.descriptionVehicule}}</p>
              <p class="card-text"><small class="text-muted">{{ value.titreAgences  }} - {{ value.ville }} - {{ value.cp }} </small></p>
              <p class="card-text"><small class="text-muted">{{ value.totalPrice  }} € </small></p>
              <p class="card-text">du <span class="text-danger">{{value.dayStart | date('d:m:Y') }}</span> au <span class="text-danger">{{value.dayEnd | date('d:m:Y')}}</span></p>
            </div>
            <div class="d-flex flex-end">
                <a href="{{ path('deleteUserCommande' , { 'id' : value.idCommande }) }}" title="Annuler la commande"><svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="red" class="bi bi-trash3-fill" viewBox="0 0 16 16">
                <path d="M11 1.5v1h3.5a.5.5 0 0 1 0 1h-.538l-.853 10.66A2 2 0 0 1 11.115 16h-6.23a2 2 0 0 1-1.994-1.84L2.038 3.5H1.5a.5.5 0 0 1 0-1H5v-1A1.5 1.5 0 0 1 6.5 0h3A1.5 1.5 0 0 1 11 1.5Zm-5 0v1h4v-1a.5.5 0 0 0-.5-.5h-3a.5.5 0 0 0-.5.5ZM4.5 5.029l.5 8.5a.5.5 0 1 0 .998-.06l-.5-8.5a.5.5 0 1 0-.998.06Zm6.53-.528a.5.5 0 0 0-.528.47l-.5 8.5a.5.5 0 0 0 .998.058l.5-8.5a.5.5 0 0 0-.47-.528ZM8 4.5a.5.5 0 0 0-.5.5v8.5a.5.5 0 0 0 1 0V5a.5.5 0 0 0-.5-.5Z"/>
              </svg></a>

            </div>

          </div>
        </div>
      </div>    
{% endfor %}

{% else %}
    <p class="text-danger text-center fs-2">Nothing</p>
{% endif %}

{% endblock body %}